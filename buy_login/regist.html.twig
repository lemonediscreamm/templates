<!--
*ファイルパス:C:\xampp\htdocs\DT\templates\buy_login\regist.html.twig
*ファイル名:regist.html.twig
*アクセスURL:http://localhost/DT/templates/buy_login/regist.html.twig
-->
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <script
        src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
        <script src ="{{constant('login\\Bootstrap::APP_URL')}}js/common.js"></script>
        <link href="{{constant('login\\Bootstrap::APP_URL')}}css/style.css"
        rel="stylesheet">
        <title>ログイン</title>
    </head>
    <body>
        <input type="hidden" name="entry_url" id="entry_url"
        value="{{constant('login\\Bootstrap::ENTRY_URL')}}">
        <input type="hidden" name="password" id="password"
        value={{dataArr.email}}>
        <input type="hidden" name="email" id="email"
        value={{dataArr.password}}>
        <form method="post" action="confirm.php">
            <table>
            {{err}}
                <tr>
                    <th>ユ－ザーID<span class="red">*</span></th>
                    <td>
                        <input type="text" name="email" value="{{dataArr.email}}">(メールアドレス)
                        {% if errArr.email | length > 0 %}<br><span class="red">
                        {{ errArr.email }}</span>{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>パスワード<span class="red">*</span></th>
                    <td>
                        <input type="password" name="password" value="">
                        {% if errArr.password | length > 0 %}<br><span class="red">
                        {{ errArr.password }}</span>{% endif %}
                    </td>                
                </tr>      
            </table>
            <div>
                <button type="button" onclick='location.href="http://localhost/DT/buy/list.php"'>一覧戻る</button>                 
                <input type="submit" name="confirm" value="ログイン">               
            </div>                        
        </form>    
    </body>
</html>
