<!DOCTYPE html>
<html>
    <head>
        <div id="wrapper1" style="background: #990000;">
            <form action="" method="get">
                <strong><span style="color: #FFFFFF;"><font size="5">ふるさとをチョイス</font></span></strong>
                <input type="text" name="search" value="">
                <input type="submit"value="検索">
            </form> 
        </div> 
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <link href="{{constant('buy\\Bootstrap::APP_URL')}}css/shopping5.css" rel="stylesheet">
    </head>
    <body>
        {# トークン #}
        <form method="post" action="cart.php">
            <input name="chkno" type="hidden" value={{chkno}}>
        </form>
        <div id="wrapper1">
            <div id="cart_list">
                <p>ショッピングカート</p>
                <p><a href="{{constant('buy\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
                <p>カート内商品数：{{sumNum}}個　合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p>
                {% if dataArr | length == 0 %}
                    <p>カートに商品は入っていません</p>
                {% else %}
                    {% for value in dataArr %}
                        <div class="item">  
                            <ul>
                                <li class= "image"><img src="{{constant('buy\\Bootstrap::ENTRY_URL')}}images/{{value.image}}"alt="{{value.item_name}}"></li>                               
                                <li class="name">{{value.item_name}}</li>
                                <li class="price">&yen;{{value.price|number_format(0, '.', ',')}}</li>
                                {% for value1 in cartData %}
                                    {% if value1.item_id == value.item_id %}
                                        <li>{{value1.count}}個</li>
                                    {% endif %}
                                {% endfor %}                   
                                <li><a href="{{constant('buy\\Bootstrap::ENTRY_URL')}}cart.php?crt_id={{value.crt_id}}">1個減らす</a></li>    
                        　　</ul>
                        </div>
                    {% endfor %}
                {% endif %}        
            </div>                                            
        </div>    
    </body>
</html>               
