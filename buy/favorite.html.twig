<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="{{constant('buy\\Bootstrap::APP_URL')}}css/shopping5.css"
        rel="stylesheet">
    </head>
    <body>
        <div id="wrapper">
            <div id="favorite">
                <p>お気に入り一覧</p>
                <p><a href="{{constant('buy\\Bootstrap::ENTRY_URL')}}list.php">商品一覧へ戻る</a></p>
                {# <p>お気に入りの数：{{sumNum}}個　合計金額：&yen;{{sumPrice|number_format(0, '.', ',')}}</p> #}
                {% if item_data | length == 0 %}
                    <p>お気に入りはありません</p>
                {% else %}   
                    {% for value in item_data %}
                    <form action="favorite.php" method="post">
                        <div class="item">
                            <ul>
                                <li class= "image"><img src="{{constant('buy\\Bootstrap::ENTRY_URL')}}images/{{value.image}}"alt="{{value.item_name}}"></li>
                                <li class="name">{{value.item_name}}</li>
                                <li class="price">&yen;{{value.price|number_format(0, '.', ',')}}</li>
                                <li><a href="{{constant('buy\\Bootstrap::ENTRY_URL')}}favorite.php?item_id={{value.item_id}}">削除</a></li>
                        　　</ul>
                        </div>
                    </form>   
                    {% endfor %}
                {% endif %}    
            </div>                                            
        </div>
    </body>
</html>            