<!--
*ファイルパス:C:\Users\lemon\Downloads\pleiades-2018-09-php-win-64bit-jre_20181004\pleiades\xampp\htdocs\DT\templates\buy\confirm.html.twig
*ファイル名:confirm.html.twig
-->
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        <title>更新内容確認</title>
        <p><font size="5">更新内容</font></p>
    </head>
    <body>
        <form method="post" action="update.php">
            <table>
                <tr>
                    <th>お名前（氏名）</th> 
                    <td>{{dataArr['family_name']}} {{dataArr['first_name']}}</td>
                </tr>
                <tr>
                    <th>お名前（かな）</th> 
                    <td>{{dataArr['family_name_kana']}} {{dataArr['first_name_kana']}}</td>
                </tr>    
                <tr>
                    <th>性別</th>
                    <td>
                        {% if dataArr['sex'] == 1 %}男性{% elseif dataArr['sex'] == 2 %}女性{% endif %}
                    </td>
                </tr>
                <tr>
                    <th>生年月日</th>  
                    <td>{{dataArr['year']}}年 {{dataArr['month']}}月 {{dataArr['day']}}日</td>
                </tr>
                <tr>
                    <th>郵便番号</th>  
                    <td>{{dataArr['zip1']}} - {{dataArr['zip2']}}</td> 
                </tr>                    
                <tr>
                    <th>住所</th>
                    <td>{{dataArr['address']}}</td>
                </tr>               
                <tr>
                    <th>電話番号</th>    
                    <td>{{dataArr['tel1']}} - {{dataArr['tel2']}} - {{dataArr['tel3']}}</td> 
                </tr>
                <tr>
                    <th>メールアドレス</th>  
                    <td>{{dataArr['email']}}</td> 
                </tr> 
            </table>
            <div>
                <input type="button" name="back" value="もう一度編集する" onclick="history.back(); return false;"> 
                <button type="button" onclick='location.href="http://localhost/DT/buy/list.php"'>一覧戻る</button>              
                <input type="submit" name="update" value="更新する">
                {% for key, value in dataArr %}
                    <!--↓下記メソッドで配列か否かをチェックしている -->
                    {% if value is iterable %}
                        {% for v in value %}
                            <input type="hidden" name="{{key}}[]" value="{{v}}">
                        {% endfor %}
                    {% else %}
                            <input type="hidden" name="{{key}}" value="{{value}}">
                    {% endif %}
                {% endfor %}
            </div>
        </form>
    </body> 
</html>               

